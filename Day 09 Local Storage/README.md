# Day 09 – Store Form Data in localStorage 💾

## 📌 Description
This project demonstrates how to **store user form data in the browser’s localStorage** using **JavaScript** as part of my **30-Day JavaScript Challenge**.The form collects user details, validates the input, and securely stores the data in localStorage so that it remains available even after the page is refreshed.

---

## ✨ Features
- Collects user input through a form
- Validates all fields before saving
- Stores form data in browser localStorage
- Displays success and error messages
- Data persists after page refresh

---

## 🛠️ Technologies Used
- HTML
- CSS
- JavaScript

---

## 🧠 Concepts Learned
- localStorage 
- Storing  data using `setItem()`
- Converting objects to strings using `JSON.stringify()`
- Form handling using `onsubmit`
- Event object and `preventDefault()`
- Basic input validation
- DOM manipulation

---

## Status
✅ Day 09 completed